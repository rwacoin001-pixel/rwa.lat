# API-008 订单、执行、结算与争议状态机

**状态：** 本地 Demo 闭环完成，待生产验收  
**更新：** 2026-07-15

## 本轮完成

- 新增 `app.orders`、`order_events`、`positions`、`yield_batches`、`yield_allocations` 与 `prediction_settlements` 模型；金额均以最小单位整数保存。
- 已实现 Bearer 会话下的下单、报价/准入校验、幂等键、可用余额锁定、Demo 管理端成交推进，以及成交后持仓、账本、通知同步。
- 四类产品（RWA、AI Compute、Global Stocks、Prediction）均由同一服务端订单状态机处理；预测市场按产品只允许结算一次。
- 已接入收益批次 `draft → previewed → approved → completed`、持仓收益分配和赎回完成。赎回会扣减持仓、记入用户可用余额，并保留账本业务引用。
- 新增本地 Demo 支持工单/争议时间线、管理员回复、推荐绑定与双方奖励记录；这些记录均落 PostgreSQL。

## 本地验收证据

- `apps/api/scripts/verify-full-demo.mjs` 每次创建新用户，完成：注册/重登、KYC 批准、HMAC 入金、四类订单成交、收益、预测结算、赎回、内部转账、提现完成、客服回复、推荐奖励、营销偏好和通知全读。
- 2026-07-15 最近一次结果：4 笔订单成交、1 个收益分配、锁定余额 `0`、收款人到账 `10000000` 最小单位、客服状态 `investigating`、推荐双方各有 1 条奖励、未读通知 `0`。
- `apps/api` 全量 Jest：29 个套件、146 个测试通过；Nest 构建通过。Jest 保留既有 open-handle 警告，不影响测试断言结果。

## 生产前仍需完成

- Demo 管理端接口必须替换为 API-013 RBAC、双人审批与审计身份，不得在生产环境开放。
- 真实经纪/托管/交易伙伴订单映射、异步回调、部分成交、撤单、争议证据、链上结算和对账仍依赖已签约伙伴与正式运行环境。
- 订单与资金相关的生产安全门禁、限额、风控和回调重放演练应按 `Code-Remediation-Execution-Plan-2026-07-14.md` 完成。
