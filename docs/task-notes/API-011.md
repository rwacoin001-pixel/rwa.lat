# API-011 任务备注

## 依赖与范围
- 依赖：API-004、API-006、API-010（Task-Board 标注）
- 范围（Task-Board）：AI 编排——结构化建议、证据、来源、数据时点、安全拒绝与成本记录。
- 核心约束：AI 只能提出建议，无权执行资金操作。

## 本轮实现范围说明
- 本轮（与 DB-005 同批）仅实现 **API-011 的"通知"子集**：
  - `GET /v1/notifications?recipient_user_id=&channel=&kind=&filter=`
  - `POST /v1/notifications`（创建通知）
  - `POST /v1/notifications/:id/read`（标记已读）
  - 本地错误码 `NOTIFICATION_ERROR_CODES`（不入侵 packages/contracts，守"不改现有代码"红线）。
- **未做**：AI 编排（结构化建议/证据/来源/成本记录）。该部分依赖 API-010（持仓），按依赖链留待 API-010 完成后补。

## DESIGN.md 阅读记录
- 已阅读 DESIGN.md 与 Task-Board 完成定义。API-011 强约束"AI 无资金权限、每笔交易独立确认"——本子集为纯通知读取，无资金动作，符合约束。

## 验证
- 见 DB-005.md（notification.integration / service 两套测试全绿）。
- build 通过。

## 待审核事项
- AI 编排部分：待 API-010 完成后补齐，届时单独验证。
- 通知 controller 未接鉴权守卫（AuthGuard）——本轮未引入 auth 模块（避免触碰现有半成品安全代码）；接网关鉴权时再补 guard。
