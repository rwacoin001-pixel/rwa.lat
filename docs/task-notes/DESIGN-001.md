# DESIGN-001 前台核心原型：首页与项目详情内容增量

**状态：** 进行中  
**更新日期：** 2026-07-12  
**设计前置：** 已完整阅读 `DESIGN.md`。未修改锁定的 `Brand`、`TopBar`、`BottomDock` 或详情流导航规则。

## 本次完成内容

- 首页新增可操作的市场摘要、今日待办、收益日历、匹配项目与近期活动模块；所有入口均进入现有页面或给出明确 Demo 反馈。
- `lib/demo-catalog.ts` 为 12 个演示项目补充独立资料：发行主体/载体、结构、参考地点、目标规模、结算方式、投资逻辑、现金流依据、风险、项目节点和文件清单。
- RWA、算力、股票与预测市场详情页共用 `ProjectIntelligence` 信息模块，避免仅替换名称却仍使用同一份详情文案。
- 所有新数据均标记为 `Demo` 或 `Illustrative`；不表示真实发行、托管、收益承诺或外部订单执行能力。

## 验证

- 已运行 `npm run build`，Next.js 生产构建通过。
- 运行态检查返回 200：`/home`、`/invest`、`/invest/rwa`、`/invest/compute`、`/invest/stocks`、`/portfolio`、`/wallet`。

## 尚未完成的边界

- DESIGN-001 仍需按照任务板要求补齐正式概念长图与设计素材可追溯说明后才能进入待审核。
- 订单的部分成交/失败/报价过期、KYC 审核中/拒绝/补件，以及全局离线/维护/错误状态仍属于 `FE-006`、`FE-010`、`FE-015` 的后续范围。
