# FE-006 认证与账户恢复原型验收备注

**状态：** 原型完成  
**更新日期：** 2026-07-12  
**设计前置：** 已完整阅读 `DESIGN.md`，并以 `01-onboarding.png`、`02-sign-in.png`、`03-kyc.png` 为视觉合同；未修改锁定的主应用导航规则。

## 本轮完成

- 新增 `components/auth-experience.tsx` 与 CSS Module，正式接管 `/welcome`、`/login`、`/register`、`/verify-email`、`/recovery`、`/profile/kyc`。
- 覆盖邮箱校验、密码与条款、Google/X/钱包演示加载、邀请码、OTP 过期与重发、账户恢复确认。
- KYC 覆盖证件、活体、资格三阶段，以及错误、重试和完成状态。
- 视觉包含品牌轨道、资产星系、液态毛玻璃、护照与面部扫描，并支持 reduced-motion。

## 验证与边界

- `npm run build` 生产构建通过。
- 当前为高保真前端原型；真实会话、OAuth、钱包签名、KYC 提交和权限回跳仍必须由 API-002/API-003/API-004 驱动。
