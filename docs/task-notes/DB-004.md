# DB-004 任务备注

## 依赖与范围
- 依赖：DB-001（app schema 基础）
- 范围：资产、产品、文件、报价、订单、执行、持仓、收益与结算模型（本任务实现：资产类别、产品、披露文件、报价、价格快照；订单/执行/持仓/收益/结算随 API-008/API-010 后续迁移补齐）
- 核心约束：四类资产（rwa/compute/stocks/prediction）共用核心状态模型；产品配置可版本化（version 列 + UNIQUE(id, version)）

## DESIGN.md 阅读记录
- 已阅读 DESIGN.md（视觉/导航/组件规范）与 Task-Board 完成定义第 1、6 条。
- DB-004 相关强约束来自 Task-Board："四类资产可共用核心状态模型；产品配置可版本化"。API-006 强约束："数据过期不能下单"。

## 实现内容
- 迁移 `1783765000000-create-catalog-model.ts`：新建 `app.asset_classes`、`app.products`、`app.disclosure_files`、`app.price_quotes`、`app.price_snapshots`。
  - products 含 `asset_class_id` 外键、`version`（可版本化）、`state` 状态机（draft/published/suspended/retired）、network、min/max_order_atomic_amount、CHECK（min<=max）。
  - price_quotes 含 `valid_until`、`captured_at` 与 `CHECK(valid_until > captured_at)`（新鲜度窗口）。
- 实体：`asset-class.entity.ts`、`product.entity.ts`、`disclosure-file.entity.ts`、`price.entity.ts`（5 张表映射）。
- 本地错误码 `catalog.errors.ts`（PRODUCT_NOT_FOUND / ASSET_CLASS_NOT_FOUND / DISCLOSURE_NOT_FOUND / QUOTE_STALE / PRODUCT_NOT_PUBLISHED），不入侵共享 `packages/contracts`（遵守"不改现有代码"红线）。
- `CatalogModule` 注册进 `app.module.ts`（仅新增 import + imports 一行）。

## 验证结果
- `nest build` 退出码 0（整个 catalog 模块编译通过）。
- 单元测试 `test/catalog/catalog.service.spec.ts`：5 passed（产品缺失抛错、报价新鲜度标记、数据过期拒绝下单、未发布拒绝）。
- 集成测试 `test/catalog/catalog.integration.spec.ts`：3 passed（PG 实库 rwa_lat_test，迁移建表 + 完整流程 + 过期拒绝 + 披露过滤）。
- 测试隔离策略：`beforeAll` 采用 `DROP SCHEMA app CASCADE` + `DROP TABLE public.schema_migrations` + `runMigrations`，避免跨 run 数据残留与 TypeORM 误判 DB-001 已应用。

## 备注
- 未提交代码（按"只实现+跑通、先不入库"的节奏，待产品负责人确认后入库）。
- 未改动任何现有源码（wallet/ledger/security/compliance/identity 等均未动，仅 `app.module.ts` 新增 CatalogModule 注册）。
