# DB-005 任务备注

## 依赖与范围
- 依赖：DB-001（app schema 基础）
- 范围：通知、工单、邀请、订阅、费用、奖励和偏好模型；均具备归因、版本、撤销与审计字段。
- 核心约束（Task-Board）：四类资产外的运营/通知数据模型，需可审计、可撤销。

## DESIGN.md 阅读记录
- 已阅读 DESIGN.md 与 Task-Board 完成定义。DB-005 的强约束为"具备归因、版本、撤销和审计字段"。
- 实施要点：
  - 所有状态表均带 `状态 ↔ 时间戳` 互斥 CHECK（如 `state='accepted' = (accepted_at IS NOT NULL)`），保证状态机一致、可审计。
  - 金额列复用 DB-003 定义的 `app.atomic_unit_amount` DOMAIN（numeric(78,0)），与账务体系对齐。
  - 偏好表 `channels` 为 jsonb，默认仅 in_app 开启，符合"默认最小化通信"原则。

## 验证（本机 PG 实库，rwa_lat_test）
- `nest build` 通过。
- `test/notification/notification.integration.spec.ts` 3/3：迁移建 7 表、通知 create/list/markRead 生命周期、未知 id 抛 NOTIFICATION_NOT_FOUND。
- `test/notification/notification.service.spec.ts` 5/5：listForUser unread/read 过滤、markRead 幂等、create 持久化、NOT_FOUND。

## 待审核事项
- API-011 本次仅实现"通知"子集（查询/创建/标记已读 + controller）；其 Task-Board 标题为"AI 编排"，完整 AI 建议/证据/成本记录依赖 API-010（持仓），留待后续。
- 工单/邀请/订阅/费用/奖励/偏好 仅建模型与实体，未暴露 controller（非 API-011 本任务范围，按需后续）。
