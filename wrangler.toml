name = "rwalat"
compatibility_date = "2026-07-09"

# Static-exported Next.js site served as Worker assets (no Worker script needed).
# Matches the dashboard project name "rwalat"; deploy command stays `npx wrangler deploy`.
[assets]
directory = "./out"
